# Topic Spinner

Daily random tech topic generator to fuel a daily blog habit.

## Status
Frontend built against fake data (see `lib/fake-data.ts`). Database, seed script,
and server actions are stubbed and not yet wired — that's the next phase.

## Run locally
npm install
npm run dev
